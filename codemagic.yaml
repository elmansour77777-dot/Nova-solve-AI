    workflows:
      release:
        name: Release
        instance_type: mac_mini_m1
        environment:
          flutter: stable
        scripts:
          - flutter build apk --release
        artifacts:
          - build/app/outputs/flutter-apk/app-release.apk
